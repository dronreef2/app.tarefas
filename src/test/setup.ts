import '@testing-library/jest-dom/vitest'

// localStorage in happy-dom is available by default. Add browser-API
// polyfills here as the surface grows (e.g. IntersectionObserver,
// ResizeObserver, matchMedia).
